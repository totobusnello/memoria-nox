name: SDK — .NET

on:
  push:
    paths:
      - "sdk/dotnet/**"
      - ".github/workflows/sdk-dotnet.yml"
  pull_request:
    paths:
      - "sdk/dotnet/**"
      - ".github/workflows/sdk-dotnet.yml"

jobs:
  build-and-test:
    name: .NET ${{ matrix.dotnet }} build + test
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        dotnet: ["8.0"]
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Set up .NET ${{ matrix.dotnet }}
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ matrix.dotnet }}

      - name: Restore dependencies
        working-directory: sdk/dotnet
        run: dotnet restore

      - name: Build
        working-directory: sdk/dotnet
        run: dotnet build --no-restore --configuration Release

      - name: Test
        working-directory: sdk/dotnet
        run: dotnet test --no-build --configuration Release --verbosity normal --logger "trx;LogFileName=test-results.trx"

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dotnet-test-results-${{ matrix.os }}-${{ matrix.dotnet }}
          path: "**/*.trx"

  # ── Future: publish to NuGet on release tags ─────────────────────────────
  # publish:
  #   name: Publish to NuGet
  #   runs-on: ubuntu-latest
  #   needs: build-and-test
  #   if: startsWith(github.ref, 'refs/tags/sdk-dotnet-v')
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-dotnet@v4
  #       with:
  #         dotnet-version: "8.0"
  #     - name: Pack
  #       working-directory: sdk/dotnet/NoxMem.Client
  #       run: dotnet pack --configuration Release --output nupkgs
  #     - name: Push to NuGet
  #       working-directory: sdk/dotnet/NoxMem.Client
  #       run: dotnet nuget push nupkgs/*.nupkg --api-key ${{ secrets.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json
