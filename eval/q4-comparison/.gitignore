# Q4 COMPARISON harness — local-only artifacts
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.env
.env.q4

# Corpus loader cache (parsed JSONL from HF + raw downloads). Large, regenerable.
cache/

# mem0 Chroma persistent stores (rc4 + smoke). Large, regenerable from re-ingest.
.mem0-chroma*/
