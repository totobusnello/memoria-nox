# G5 Ablation Runbook — 2026-05-20

> **Purpose**: re-medir contribuição real da boost stack pós-Wave-A-fixes (PRs #150 + #151).
>
> **Pre-deploy state** (G4, 2026-05-19): A8 canonical = 0.5702 nDCG@10, A3 (section) peak = 0.6222, salience active piora vs shadow (-0.01), tier_boost piora vs baseline (-4%), source_type INERT (98.48% NULL).
>
> **Expected pós-deploy lifts** (Wave A fixes):
> - A8' > 0.5702 (salience aditivo evidence-weighted captures real signal)
> - A8' > A7' (active beats shadow with new formula)
> - A5' > A0 (source_type ALIVE post-backfill)
> - A9' ≤ A8' (tier still over-promotes, confirms PR #150 default-off decision)

## Pre-requisites

- [x] PR #150 merged (commit `429d1fd`) — salience aditivo + tier_boost off
- [x] PR #151 merged (commit `e228242`) — source_type backfill script
- [ ] **Deploy VPS** via DEPLOY-WAVE-A pattern (rsync + tsc + restart)
- [ ] **Backfill source_type executed em prod** via `nox-mem backfill-source-type`
- [ ] **DB clone isolado** em `/root/.openclaw/workspace/eval-data/g5-2026-05-20/g5.db`

## Step-by-step

### 1. Deploy Wave A + Backfill source_type

SSH manual no VPS (lições agent stall — Agent tool falha em multi-phase pipelines):

```bash
ssh root@srv1465941
cd /root/.openclaw/workspace/tools/nox-mem

# Pull latest main
git pull origin main

# Deploy staged-1.7a/edits → src/
rsync -av staged-1.7a/edits/salience.ts src/lib/salience.ts
rsync -av staged-1.7a/edits/search.ts src/search.ts
rsync -av staged-1.7a/edits/backfill-source-type.ts src/backfill-source-type.ts

# Build + restart
set -a; . /root/.openclaw/.env; set +a
npx tsc
systemctl restart nox-mem-api
sleep 5
curl -s http://127.0.0.1:18802/api/health | jq .status
# Expected: "ok"
```

### 2. Wire CLI subcommand (separate manual step pre-Task F runtime)

Add to `src/index.ts` switch statement (CLI dispatcher):

```typescript
case "backfill-source-type": {
  const { backfillSourceType, parseArgs, formatResult } = await import("./backfill-source-type.js");
  const opts = parseArgs(process.argv.slice(3));
  const result = await backfillSourceType(opts);
  console.log(formatResult(result));
  break;
}
```

Rebuild + restart, then proceed.

### 3. Dry-run backfill

```bash
nox-mem backfill-source-type --dry-run
```

**Expected output:**
```
[DRY-RUN] Backfill complete: 67949/67949 chunks in Xs

Distribution:
  note          ~31000  (~46%)
  personal-doc  ~23000  (~34%)
  ocr-cache     ~11000  (~16%)
  entity            749  (1.10%)
  project-doc       560  (0.82%)
  ...
```

Validate distribution matches `docs/audits/2026-05-19-source-type-backfill-mapping.md` projections. Se desviar muito (>10% em qualquer bucket), investigar antes do apply.

### 4. Apply backfill em prod

```bash
nox-mem backfill-source-type
# Aguarda snapshot pre-op + audit row via withOpAudit
# Pós-completion, validate:
sqlite3 /root/.openclaw/workspace/tools/nox-mem/nox-mem.db \
  "SELECT source_type, COUNT(*) FROM chunks GROUP BY source_type ORDER BY 2 DESC"
# Expected: NULL count drops from 67,949 → ~0, distribution matches dry-run
```

### 5. Clone isolated DB pra G5 ablation

```bash
mkdir -p /root/.openclaw/workspace/eval-data/g5-2026-05-20
cp /root/.openclaw/workspace/tools/nox-mem/nox-mem.db \
   /root/.openclaw/workspace/eval-data/g5-2026-05-20/g5.db
ls -la /root/.openclaw/workspace/eval-data/g5-2026-05-20/g5.db
# Expected: same size as prod DB (~1.2GB)
```

### 6. Stage eval scripts em /tmp

```bash
# Run script:
scp tools/eval/run-g5.sh root@srv1465941:/tmp/run-g5.sh
scp tools/eval/g5-eval.py root@srv1465941:/tmp/g5-eval.py
ssh root@srv1465941 'chmod +x /tmp/run-g5.sh /tmp/g5-eval.py'
```

### 7. Run G5 ablation (12 configs, ~50min wall, ~$0.30-0.40)

```bash
ssh root@srv1465941
bash /tmp/run-g5.sh
# Tail output:
tail -f /tmp/g5-results.log
```

### 8. Compare vs G4 baseline

| Config | G4 nDCG@10 | G5 Expected | G5 Actual |
|---|---|---|---|
| A0 FTS5 | 0.4817 | ~0.48 (stable) | TBD |
| A1 Semantic | 0.5702 | ~0.57 (stable) | TBD |
| A2 Hybrid no boost | 0.5739 | ~0.57 (stable) | TBD |
| A3 section only | 0.6222 | ~0.62 (stable) | TBD |
| A4 BOOST_TYPES only | 0.5348 | ~0.53 (stable) | TBD |
| A5 source_type only | 0.4817 (INERT) | **>0.50** (alive) | TBD |
| A6 tier only | 0.4616 (piora) | ~0.46 (still piora) | TBD |
| A7 full + shadow | 0.5805 | TBD | TBD |
| **A8 full + active CANONICAL** | **0.5702** | **>0.60** | TBD |
| A9 full + active + tier | NEW | ≤ A8 | TBD |
| A10 full minus source_type | NEW | < A8 | TBD |
| A11 full minus section | NEW | << A8 | TBD |

### 9. Update HANDOFF + Memory

- HANDOFF.md: add G5 section com tabela completa + decision tree
- Save memory: `project_g5_wave_a_post_backfill_results.md`
- Update `project_g4_wave_a_results_2026_05_19.md` cross-link
- If A8' meets expected lift: update visual identity + paper §5 com number canonical

## Decision tree (post-G5)

- **A8' ≥ +10% vs G4 A8** → Wave A success cravado, paper §5 reframe definitivo
- **A8' +5-10%** → partial success, investigar quais components contribuíram mais
- **A8' <+5%** → unexpected — debug ablation, possivelmente salience aditivo precisa W tuning

## Rollback plan

Se G5 mostra regression vs G4:
1. Identificar config offender (A8 vs G4 A8 delta = primary metric)
2. Toggle individual env flags em prod (NOX_DISABLE_SOURCE_TYPE_BOOST, NOX_SALIENCE_MODE=shadow)
3. Hot rollback NÃO requer redeploy — todos flags são runtime
4. Se needed redeploy old src: git checkout `429d1fd^` for pre-#150 state

## Cross-links

- G4 results: `memory/project_g4_wave_a_results_2026_05_19.md`
- Audit: `docs/audits/2026-05-19-salience-distribution-audit.md`
- Mapping: `docs/audits/2026-05-19-source-type-backfill-mapping.md`
- PR #150: commit `429d1fd` (salience aditivo)
- PR #151: commit `e228242` (source_type backfill)
- Agent stall pattern: `memory/feedback_agent_stall_on_multi_phase_pipelines.md`
