# Plan — OpenClaw v2026.4.25 Upgrade + Context Reduction + Runbook Capture

> Created: 2026-04-27 | Status: AWAITING-GO | Owner: Toto + Claude session
> Prerequisites synthesized from 4 parallel reconnaissance agents (audit, binary diff, community signals, release-notes correlation)
> Filosofia: **forward-fix > rollback** · **slow + observable > fast** · **simplify only after stable**

---

## North Star

1. **Não quebrar nada que já funciona** (CLI billing zero, monkey-patch fratricide, immutable wrapper, env hygiene)
2. **Aproveitar wins nativos da v.25** pra retirar hacks redundantes (bedrock physical-mv, doctor auto-repair, plugin discovery)
3. **Reduzir contexto de boot dos agents** (config slim, plugins lean, transcripts curtos)
4. **Sair com runbook reutilizável** pra v.26+ — nunca mais improvisar upgrade

---

## Pre-flight Invariantes (NÃO podem regredir)

| # | Invariante | Validação |
|---|---|---|
| I1 | `agents.defaults.model.primary = claude-cli/claude-opus-4-6` | `openclaw config get` |
| I2 | `agents.defaults.cliBackends.claude-cli` **não existe** | `openclaw config get` retorna null/error |
| I3 | Fallback chain SEM `anthropic/*` direto | `openclaw config get agents.defaults.model.fallbacks` |
| I4 | Monkey-patch #62028 em `dist/restart-stale-pids-*.js` | grep marker comment + função body |
| I5 | Wrapper `/usr/local/bin/openclaw-gateway-wrapper` imutável | `lsattr` mostra `i` |
| I6 | systemd drop-in `Environment=IS_SANDBOX=1` | `cat override.conf` |
| I7 | `CLAUDE_CODE_OAUTH_TOKEN` comentado no `.env` | grep `^#DISABLED_CLAUDE_CODE` |
| I8 | bedrock plugin ausente do node_modules | `ls openclaw/node_modules \| grep -i bedrock` vazio |
| I9 | nox-mem 3 services active (gateway, api, watcher) | `systemctl is-active` |
| I10 | vectorCoverage 100% (ou conhecido) | `/api/health.vectorCoverage` |
| I11 | sessions.json model field == `claude-*` em todos 6 agents | jq filter |
| I12 | Node.js wrapper `/usr/bin/node` → `node.bin --no-warnings` | `head -2 /usr/bin/node` |

---

## Phase 0 — Pre-upgrade Hygiene + Context Reduction (zero risco — pode rodar HOJE antes do GO)

### 0.A Backup completo (audit trail, não rollback target)
- [ ] `tar czf /var/backups/preupgrade-v25-$(date +%Y%m%d-%H%M).tar.gz openclaw.json agents/*/agent/auth-profiles.json agents/*/sessions/sessions.json /etc/systemd/system/openclaw-gateway* /usr/local/bin/openclaw-gateway-wrapper /usr/bin/node` (read all from /root/.openclaw/ + system locations)
- [ ] Snapshot DB nox-mem: `sqlite3 /root/.openclaw/workspace/tools/nox-mem/data/nox-mem.db "VACUUM INTO '/var/backups/nox-mem-pre-v25-$(date +%Y%m%d).db';"`
- [ ] Hash atual do monkey-patch: `ls /usr/lib/node_modules/openclaw/dist/restart-stale-pids-*.js > /var/backups/preupgrade-v25-monkey-patch-path.txt`
- [ ] Dump `openclaw plugins list` + `openclaw config dump` em /var/backups/

### 0.B Resolver blockers do audit (estado pré-upgrade não-conforme)
- [ ] **Sessions stickiness** (todos 6 agents em gemini-* — rule 11 violada): for cada agent in nox/atlas/boris/cipher/forge/lex: `jq 'with_entries(select(.value.model | startswith("claude-")))' sessions.json > sessions.json.tmp && mv sessions.json.tmp sessions.json`. Se filtro deixa vazio = `echo '{}' > sessions.json`.
- [ ] **vectorCoverage 95.7% gap** (2935 orphans surgiram em 8h pós-fix de manhã): `set -a; source .env; set +a; nox-mem vectorize` foreground; validar `/api/health.vectorCoverage.embedded == total`.
- [ ] **chattr -i `.credentials.json`** (preparar pra #70902 OAuth sync ativo da v.25; sem isso EPERM silencioso): `chattr -i /root/.claude/.credentials.json` + backup antes `cp .credentials.json .credentials.json.pre-v25`.

### 0.C Context reduction — slim down boot footprint
- [ ] **auth-profiles.json slim em nox + forge** (hoje 7 providers cada vs 2 nos demais). Backup, depois reduzir pra `anthropic-max:default + anthropic:default` apenas (matching atlas/boris/cipher/lex). Providers extras (google/openai/openai-codex/openrouter) só são úteis se algum agent específico usa — confirmar com Toto antes de cortar.
- [ ] **Plugin audit** — hoje audit reportou `54/101 plugins loaded`. Listar via `openclaw plugins list` + categorizar:
  - **Core (manter):** claude-cli (built-in), graph-memory, nox-mem, channels Discord/Telegram em uso, system plugins
  - **Não usado (disable):** identificar candidates (TTS providers exóticos, channels não usados, etc); `openclaw plugins disable <id>` via CLI (NÃO jq)
  - **Já removido fisicamente:** bedrock — confirmar que não voltou
  - Output: lista enxuta de plugins disabled + razão; alvo: <40 loaded
- [ ] **HEARTBEAT.md size audit** — `wc -l agents/*/HEARTBEAT.md` + ler samples; se houver instruções repetidas que poderiam virar skill/system-prompt-once, encurtar. Não bloqueante pro upgrade.
- [ ] **Cron review** — hoje 23 jobs ativos. Identificar redundantes ou que poderiam consolidar. Ex: 2 monitors de config drift (`*/30` + `*/10`) — manter o de 30min, remover o de 10min se redundante.

### 0.D Pre-stage v.25 (não instala ainda)
- [ ] `OPENCLAW_SERVICE_REPAIR_POLICY=external` adicionado ao `/etc/systemd/system/openclaw-gateway.service.d/override.conf` (NÃO restart ainda)
- [ ] `npm view openclaw@2026.4.25` confirma versão alvo + `dist-tags`
- [ ] Download manual sem instalar: `npm pack openclaw@2026.4.25 --pack-destination /var/cache/openclaw-v25/` — permite inspeção pre-install
- [ ] Inspecionar tarball: `tar tzf /var/cache/openclaw-v25/openclaw-*.tgz | grep -E 'restart-stale-pids|cliBackends|agentRuntime|plugins/installs'` — confirma que arquivos esperados estão lá
- [ ] **Gate de aprovação 0:** mostrar diff do tarball vs install atual; Toto aprova passar pra Phase 1

### 0.E Health snapshot final pré-upgrade
- [ ] `curl /api/health > /var/backups/health-pre-v25.json`
- [ ] `journalctl -u openclaw-gateway --since "1 hour ago" > /var/backups/journal-pre-v25.log`
- [ ] `ps -eo pid,etime,pcpu,pmem,cmd > /var/backups/ps-pre-v25.txt`
- [ ] Confirmar todos I1-I12 passando

---

## Phase 1 — Install v2026.4.25 (1 step que muda binário, observação imediata)

- [ ] **Stop services em ordem reversa de dependência:** watcher → api → gateway. NÃO usar `systemctl stop` em paralelo.
  ```
  systemctl stop nox-mem-watcher
  systemctl stop nox-mem-api
  systemctl stop openclaw-gateway
  ```
- [ ] **Confirmar processos mortos:** `ps -ef | grep -E 'openclaw|nox-mem' | grep -v grep` deve retornar vazio em <5s
- [ ] **Install:** `npm install -g openclaw@2026.4.25` (PIN explicito, não `npm update`)
- [ ] **Validar versão:** `openclaw --version` == `2026.4.25` (output formato pode mudar — se não retornar versão clean, abort)
- [ ] **Inspect dist:** `find /usr/lib/node_modules/openclaw/dist -name 'restart-stale-pids-*.js' -ls` — captura novo hash + tamanho
- [ ] **Gate 1:** binário instalado + versão correta + dist file existe — Toto aprova Phase 2

---

## Phase 2 — Reapply Customizations (cada uma valida antes da próxima)

- [ ] **Monkey-patch #62028:** `bash /root/reapply-monkey-patch.sh` — script já é idempotente Python regex, deve detectar novo hash via glob
  - Validação: grep marker comment EM funcionar body (não `grep -c`, lição da memory `feedback_monkey_patch_grep_count_is_false_positive`)
  - Se regex não casa (release notes mencionam mudanças whitespace): paro, mostro diff, decidimos juntos
- [ ] **graph-memory log patch:** verificar se reinstalou plugin; se sim, reaplica do `.bak-log-fix-*` mais recente
  - Validação pós: gateway log no boot mostra `[graph-memory] ready ... model=gemini-2.5-flash-lite`
- [ ] **bedrock removal (oportunidade simplificação):** ao invés do `mv pra /tmp` antigo, usar nativo `openclaw plugins uninstall --force bedrock-unk` (release notes #71957 + plugins/uninstall feature)
  - Se uninstall nativo não pega bedrock por algum motivo, fallback: `mv` físico como antes
  - Validação: `openclaw plugins list | grep -i bedrock` vazio
- [ ] **Wrapper imutável:** `lsattr /usr/local/bin/openclaw-gateway-wrapper` — bit `i` ainda presente (deveria, mas confirmar)
- [ ] **systemd reload:** `systemctl daemon-reload` (drop-in com `OPENCLAW_SERVICE_REPAIR_POLICY=external` + `IS_SANDBOX=1`)
- [ ] **Gate 2:** todos hacks reaplicados + simplificações aplicadas — Toto aprova Phase 3

---

## Phase 3 — Schema Migration (com backup, NÃO cego)

- [ ] **Backup imediato pré-doctor:** `cp /root/.openclaw/openclaw.json /var/backups/openclaw-pre-doctor-v25.json`
- [ ] **Doctor dry-run primeiro:** `openclaw doctor --check 2>&1 | tee /var/backups/doctor-check-v25.log`
  - Lê o diff proposto sem aplicar
  - Esperado: relatar `agentRuntime.id` migration + plugin registry consolidation
- [ ] **Doctor fix com observação:** `openclaw doctor --fix 2>&1 | tee /var/backups/doctor-fix-v25.log`
  - Diff imediato pós: `diff openclaw-pre-doctor-v25.json /root/.openclaw/openclaw.json | head -50`
  - **Red flags:** se diff strippa `agents.defaults` ou `channels.discord` (community signal #65035), paro e investigo manual
  - **Verde:** diff só adiciona `agentRuntime.id` e talvez consolida plugin paths
- [ ] **Validar registry:** `openclaw plugins list` mostra claude-cli como plugin first-class (#71957) + bedrock ausente
- [ ] **Gate 3:** schema migrado, registry inicializado, configs criticos preservados — Toto aprova Phase 4

---

## Phase 4 — Service Bring-up Sequencial (cada um valida antes do próximo)

- [ ] **Start gateway primeiro:** `systemctl start openclaw-gateway`
- [ ] **Watch first 30s:** `journalctl -fu openclaw-gateway --since "30s ago"` — observar:
  - Versão reportada == 2026.4.25
  - claude-cli plugin loaded (`[plugins] [claude-cli] ready` ou similar)
  - Nenhum SIGTERM/restart loop (#62028 patch funcionando)
  - Nenhum 401 (auth profiles OK pós-OAuth sync)
  - `IS_SANDBOX=1` respeitado
- [ ] **Verifica health:** `curl http://127.0.0.1:18789/...` (gateway endpoint)
- [ ] **Start nox-mem-api:** `systemctl start nox-mem-api`; validate `curl http://127.0.0.1:18802/api/health` retorna JSON
- [ ] **Start nox-mem-watcher:** `systemctl start nox-mem-watcher`; validate inotify ativo
- [ ] **Gate 4:** 3 services active + health OK + zero erros nos primeiros 60s — Toto aprova Phase 5

---

## Phase 5 — Smoke Test 6 Agents Sequencial (NÃO paralelo)

Para cada agente em ordem **nox → forge → atlas → boris → cipher → lex**:
- [ ] Enviar 1 mensagem trivial via canal apropriado (Discord agents-hub ou DM)
- [ ] Validar:
  - Agent responde (não silencioso, não timeout)
  - Resposta NÃO é fallback (sem markers de gemini/codex)
  - `sessions.json` desse agent grava model `claude-*` (não fallback)
  - journalctl não mostra 401 ou auth retry
- [ ] **Se algum agent falha:** paro, investigo (token? auth-profile? cliBackends config drift?), fix forward, continuo
- [ ] **Gate 5:** todos 6 agents respondendo via claude-cli — Toto aprova Phase 6

---

## Phase 6 — Re-immutabilize + Token Hygiene

- [ ] Backup `.credentials.json` pós-OAuth-sync da v.25: `cp .credentials.json .credentials.json.post-v25-bak`
- [ ] Confirmar token ainda válido: `claude auth status` retorna `loggedIn:true`
- [ ] Validar token match (rule 13): `jq -r '.claudeAiOauth.accessToken[0:15]' .credentials.json` bate com setup-token original
- [ ] **chattr +i**: `chattr +i /root/.claude/.credentials.json`
- [ ] Smoke test 1 turn em qualquer agent pra confirmar que immutable não quebrou nada
- [ ] **Gate 6:** credentials seguras + token válido — Toto aprova Phase 7

---

## Phase 7 — Observability 30min + Final Validation

- [ ] **Watch loop 30min** com checkpoints a cada 5min:
  - `restart counter` do gateway (deveria ficar parado)
  - 401s em journalctl (deveria ser 0)
  - `/api/health` campos críticos (vectorCoverage, services, sessions ativos)
  - Discord agents-hub: heartbeats funcionando, sem "Unknown Channel" novos
  - Watcher ingest funcionando (criar arquivo dummy em memory/ e ver chunks++)
- [ ] **Validate v.25 native heartbeat behavior:** verificar que heartbeats agora vêm como "transient runtime context" não user prompt visível (release feature). Confirmar via inspection de session jsonl novo. Se confirmado, planejar retirada do nosso filtro patch.
- [ ] **Validate OAuth sync (#70902):** forçar refresh OAuth (esperar 1 ciclo) e ver se `.credentials.json` recebe update. Se sim, immutable bit conflita — voltamos a Phase 6 com plano alternativo (talvez `chattr +i` só após cada token rotation, não permanente).
- [ ] **Re-bench vector coverage + salience baseline:** comparar com pré-upgrade health snapshot
- [ ] **Gate 7:** 30min sem regressões + behaviors v.25 validados — UPGRADE CONCLUÍDO

---

## Phase 8 — Simplificações Pós-Estável (separado do upgrade — só se Phase 7 verde)

Estes são wins que só executamos com prod estável e tempo pra observar:

### 8.1 Retirar nosso filtro extractMessages (se v.25 já filtra heartbeat nativo)
- Validar 7 dias que session-distill domingo roda <30min com heartbeats ausentes do transcript
- Se confirmado: reverter `src/session-distill.ts` pro estado pre-fix-2026-04-27 (manter timeout 30min como segurança)

### 8.2 Aposentar comentário `agents.defaults.cliBackends MUST NOT exist`
- v.25 fez registry-loader autoritativo
- Atualizar rule 5 do CLAUDE.md: "claude-cli é plugin first-class via registry; config customizado em `cliBackends` ainda quebra parser, mas registry torna NEED de criar bloco zero"

### 8.3 Aposentar `chattr +i .credentials.json` (se #70902 funciona limpo)
- Após 14 dias de OAuth refresh sem incidente
- Documenta: "v.25 native sync substitui defesa manual"
- Mantém `.credentials.json.bak` como audit trail

### 8.4 Aposentar `OPENCLAW_NO_RESPAWN` (se monkey-patch + service-repair-policy resolveram)
- Se #62028 NÃO mais fratricides em produção por 30 dias
- Reduz wrapper pra mínimo

### 8.5 Sessions reset utility — turn into systemd-timer
- Hoje só rodamos quando fallback grude
- Pode virar canary `*/4h` que detecta+reseta drift automaticamente

---

## Phase 9 — Captura do Runbook Reutilizável (DELIVERABLE FINAL)

**Arquivo a criar:** `docs/RUNBOOKS/openclaw-upgrade.md`

Conteúdo (template populado durante este upgrade):

```
# OpenClaw Upgrade Runbook (v.YY → v.YY+1)

## Antes de começar
- [ ] Read release notes completas — dispara 4 reconnaissance agents em paralelo:
  1. VPS audit pré-upgrade
  2. Binary diff prediction
  3. Community regression scan (24-48h após release)
  4. Release notes correlation com nossas rules

- [ ] Espera 12-48h após release pra deixar comunidade reportar regressões

## Phase 0 — Pre-flight (zero risco)
[checklist genérico extraído do que fizemos hoje]

## Phase 1-7 — Upgrade execution
[gates A-G genéricos]

## Sinais de problema → forward-fix decisions
- Fratricide loop volta → reapply monkey-patch
- 401 silencioso → check auth-profile apiKey + sessions.json model
- Plugin não carrega → openclaw plugins list + reinstall
- Schema migration strippa config → restore openclaw.json from backup
- ...

## Invariantes (nunca regredir)
[I1-I12 listados]

## O que CADA reconnaissance agent precisa saber
[prompts gravados]
```

**Princípios documentados:**
1. **Cada upgrade ativa os 4 agents primeiro** — nunca improvisa
2. **Forward-fix > rollback** — só rollback se loop catastrófico (ex: dist files apagados)
3. **Per-step gates obrigatórios** — Toto aprova entre cada phase
4. **Backup como audit trail** — não como gatilho de reversão
5. **Simplificar só DEPOIS de estável** — Phase 8 separado

**Métricas de sucesso do método:**
- v.25 → v.26: tempo total <2h (vs 8h+ improvisado)
- Zero crashes em prod pós-upgrade
- Zero rollbacks necessários
- Cada upgrade aposenta pelo menos 1 hack se possível

---

## Riscos Conhecidos + Mitigações

| Risco | Probabilidade | Mitigação |
|---|---|---|
| Monkey-patch regex não casa post-bundle change | Média | Phase 2: paro e ajusto regex manual; tenho backup do dist |
| `doctor --fix` strippa configs (#65035 signal) | Baixa-média | Phase 3: dry-run primeiro, diff antes de aceitar |
| #70902 OAuth sync conflita com chattr +i | Alta | Phase 0.B: chattr -i preventivo; Phase 6: re-immutabilize só após validação |
| Plugin registry migration ressuscita bedrock | Baixa | Phase 2: uninstall nativo + verify list |
| Sessions stickiness gemini→claude-cli não cura | Média | Phase 0.B: reset preventivo + Phase 5 valida |
| openclaw-node systemd unit aparece no Linux | Baixa | Phase 4: `systemctl list-units \| grep openclaw-node`; se sim, drop-in com wrapper |
| Community reporta regressão grave nas próximas 12h | Média | Phase 0: WAIT até 2026-04-28 manhã antes de Phase 1 |

---

## Cronograma Proposto

- **HOJE 13-15h BRT:** Phase 0 (pré-upgrade hygiene + context reduction) — ZERO risco prod
- **HOJE 15-16h BRT:** Comunidade scan loop (re-rodar researcher agent 12h depois pra ver novos signals)
- **HOJE 22-23h BRT (low traffic) OU AMANHÃ 04-06h BRT:** Phase 1-7 (upgrade execution) — janela com user awake supervisionando
- **2026-04-28 a 2026-05-04:** observação 7 dias antes de Phase 8 simplificações
- **2026-05-04+:** Phase 9 (runbook captura) — depois de simplificações estáveis

---

## Decisões aguardando Toto antes de executar

1. **Phase 0 pode rodar AGORA?** (read-only audit + sessions reset + vectorize + chattr toggle são reversíveis e zero-risco)
2. **Hora do GO pra Phase 1?** (sugestão: hoje 22h BRT ou amanhã 04h BRT)
3. **auth-profiles slim em nox/forge:** cortar google/openai/openrouter providers OU manter? (cortar = boot mais leve, mas perde fallback se claude-cli falhar)
4. **Phase 8 timing:** começar simplificações em 7 dias OU 14 dias após upgrade?
