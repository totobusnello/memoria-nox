# Temp DBs created during validation runs
*.db
*.db-shm
*.db-wal
tmp/
.tmp/

# Embedding cache snapshots used for offline-mode-check
embedding-cache-snapshot/

# Node artifacts if this ever gets its own deps
node_modules/

# Test artifacts
reports/
*.report.json

# Mock fixture DB written by mock-nox-mem.cjs during local runs
# (each check uses its own tmpdir; this catches stray local execution)
fixtures/fixture-db.json

# OS artifacts
.DS_Store
