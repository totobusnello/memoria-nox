# Dimensionamento sobre 30 clusters — 2026-08-14 (v2)

> **Substitui `SIZING-2026-08-14.md`**, cujos três números saíram de um replay
> com defeito (estrato B re-sorteado pós-washout). Este documento roda sobre o
> corpus da extensão 1 **+ extensão 2**, com o `pilot_replay.py` corrigido.
>
> **O que ele não é:** uma decisão. As decisões seguem sendo do titular.

---

## 1. O que mudou desde a v1, e por quê

Duas coisas, de pesos muito diferentes.

**(a) O corpus cresceu: 27 → 30 epochs.** A extensão 2 adjudicou 187 episódios
(65 de censo do estrato A + 122 sorteados do complemento) nos epochs 12, 13 e
14/08, sob a seed do round `31309420`, declarada 10 minutos antes de o round
existir. Isso era o objetivo declarado — e **30 é o piso do §9, não uma folga**.

**(b) Um defeito no estimador foi corrigido — e este é o item maior.** O
`pilot_replay.py` re-sorteava o estrato B **depois** de aplicar o washout,
contra o desenho declarado (`EXTENSION-SEED-2026-08-11.md`: *"1.576 de 8.194"*,
onde 8.194 é o complemento **no bruto**, que é onde a amostra foi de fato
sorteada e adjudicada — 99,3% de reprodução). O script escolhia outro conjunto
de 1.576, do qual só **1.259 tinham veredito**; os 317 restantes entravam como
`unknown`. Somava-se o peso `6.675/1.576 = 4,235`, contra os **5,2×** que o
próprio desenho declara como alvo. Dois erros no mesmo sentido.

Corrigido em `db55c8f`, validado por convergência byte a byte entre o caminho
`--seed-b` corrigido e o novo `--estrato-b-ids`.

| | v1 (bug, 27 ep) | corrigido, 27 ep | **v2 (30 ep)** |
|---|---|---|---|
| oportunidades `unknown` | 232 | 44 | **50** |
| `r̂` | 22,78 | 27,86 | **28,58** |
| `p̂0` | 0,1310 | 0,1159 | **0,1155** |
| ICC | 0,1175 | 0,1016 | **0,0988** |
| design effect | 7,26 | — | **5,89** |

**A coluna do meio é a que importa para entender o que aconteceu.** Quase todo
o movimento veio da correção, não dos 3 clusters novos. Os clusters novos
mexeram no ICC na terceira casa.

## 2. Replay

```
python3 pilot_replay.py \
  --episodes universo-combinado.jsonl \
  --verdicts verdicts-combinado.jsonl \
  --estrato-b-ids estrato-b-ids.txt \
  --replicas 'tiebreak-rep*.jsonl' 'tiebreak-v2-rep*.jsonl' 'extensao-moonshot-cycle-*.jsonl'
```

```json
{
  "desenho": "estratificado-HT",
  "epochs_analisaveis": 30,
  "epochs_fora_do_icc": 3,
  "icc": 0.098809,
  "r_hat": 28.580638,
  "p0_hat": 0.115545,
  "oportunidades": 4447.202,
  "repeats": 513.853,
  "hours_per_epoch": 5.1867,
  "session_hours_per_epoch": 50.4667,
  "cobertura_adjudicacao": {"com_veredito": 2314, "episodios_total": 9579, "pct": 24.16,
                            "oportunidades_com_desfecho_unknown": 50},
  "regra_desfecho": "maioria estrita (>50%); empate => not_failure",
  "tau": "S1"
}
```

`aviso: 371 ids da amostra nao estao no universo/pos-washout` — **esperado e
correto**: os 1.698 IDs foram sorteados no bruto, e o washout remove parte
deles. É precisamente esse recorte que o script antes fazia errado.

## 3. Duração do estudo

Epoch de 24h, como o pré-registro especifica:

| MDE | epochs | dias |
|---|---|---|
| 20% | 244 | **244** |
| 25% | 154 | 154 |
| 30% | 104 | 104 |
| 35% | 74 | 74 |

Contra os **316 dias** da v1 a MDE 20% — a correção comprou 72 dias que nunca
foram devidos.

## 4. A alavanca do epoch curto — agora com o washout modelado

A v1 publicou uma tabela de epoch curto e **avisou que ela não modelava o
washout**. Modelando: o washout é fixo em 2h, então a fração útil de um epoch
de `H` horas é `(H−2)/H`, e as duas quantidades de exposição escalam por
`(H−2)/22` em vez de `H/24`.

| epoch | fração útil | MDE 20% | MDE 25% | MDE 30% |
|---|---|---|---|---|
| **24h** | 92% | 244 ep = **244 d** | 154 ep = 154 d | 104 ep = 104 d |
| **12h** | 83% | 290 ep = **145 d** | 182 ep = 91 d | 122 ep = 61 d |
| **8h** | 75% | 344 ep = **115 d** | 216 ep = 72 d | 146 ep = 49 d |
| **6h** | 67% | 412 ep = **103 d** | 258 ep = 64 d | 174 ep = 44 d |
| **4h** | 50% | 618 ep = **103 d** | 386 ep = 64 d | 262 ep = 44 d |

**A alavanca satura em ~6h.** Entre 6h e 4h o calendário não melhora — o
washout passa a comer metade de cada epoch e devora exatamente o ganho de
caber mais epochs no dia. A tabela da v1, sem washout, era monotônica e teria
sugerido continuar encurtando.

O que esta tabela **ainda não** modela: o carry-over do Apêndice B. Encurtar o
epoch aproxima as trocas de braço, e a premissa de que 2h bastam para lavar o
efeito foi calibrada para epochs de 24h. **Um washout de 2h num epoch de 6h é
uma afirmação mais forte do que num de 24h, e não foi verificada.** Antes de
adotar epoch curto, isso precisa de evidência própria — do contrário troca-se
um problema de calendário por um de validade interna.

## 5. O que continua em aberto

1. **IC do ICC.** Segue vindo de reconstrução, não do script. Expor
   `MS_between`/`MS_within` no `pilot_replay` é trabalho pequeno e continua
   sendo pré-requisito para decidir MDE.
2. **6 episódios da extensão 2 sem o 3º veredito** (3,2% dos 187): o `zhipu`
   falhou neles em 4 tentativas, e o registro de falha **não guarda a resposta
   crua** — `detail` e `reason` saem vazios. Viram `unknown`, o que é o
   tratamento correto, mas a causa fica desconhecida por falta de
   instrumentação.
3. **O painel não pode provar que julgou com glm-5.2.** O campo `model` grava o
   pedido, não o servido (`INCIDENTS.md#2026-08-14`). Vale para os 3.348
   vereditos `zhipu` anteriores e para os desta extensão.
4. **Dois epochs do corpus são parciais** — 11/08 (83,5%) e 14/08 — por censura
   à direita na extração. Declarado antes da adjudicação em
   `EXTENSION-2-SEED-2026-08-14.md`.

## 6. Proveniência

- Universo: `action-archive` da VPS via `extract_episodes.py` (leitura apenas),
  10.771 episódios brutos, recorte 12–14/08 = 700. Saída fora de repo público.
- `pilot_replay.py` em `db55c8f`; dedupe `(episode_id, panelist)` em `842975d`;
  regra de instabilidade em `97a2c25`.
- Seeds: extensão 1 `EXTENSION-SEED-2026-08-11.md`; extensão 2 round
  `31309420`, `randomness` `6a9b71b4…f0b57`, `seed = SHA256(randomness)` =
  `fd9b4027…aa85`.
- Corpus consolidado: 9.579 episódios, 7.182 pares `(episódio, painelista)`
  únicos.
